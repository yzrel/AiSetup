# Spring Boot API base URL (context path /api is included in default)
VITE_API_URL=http://localhost:8080/api

# Optional: Anthropic key for backend AI assist (also accepted in backend/.env)
# ANTHROPIC_API_KEY=sk-ant-...
